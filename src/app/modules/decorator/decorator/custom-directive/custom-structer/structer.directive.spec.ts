import { StructerDirective } from './structer.directive';

describe('StructerDirective', () => {
  it('should create an instance', () => {
    const directive = new StructerDirective();
    expect(directive).toBeTruthy();
  });
});
