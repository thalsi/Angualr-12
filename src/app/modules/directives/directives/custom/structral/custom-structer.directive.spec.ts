import { CustomStructerDirective } from './custom-structer.directive';

describe('CustomStructerDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomStructerDirective();
    expect(directive).toBeTruthy();
  });
});
