import { NgModule } from "@angular/core";
import { Router, RouterModule, Routes } from "@angular/router";

const router:Routes= []

@NgModule({
    imports:[RouterModule.forChild(router)],
    exports:[]
})
export class ngMoulePractiveRoutingMoulde { }